name: Microservice Workflow

on:
  push:
    branches:
      - main

jobs:
  call-shared-workflow:
    uses: Hitham07/source/main/.github/workflows/shared.yaml@main
    with:
      example_input: 'some-value'

